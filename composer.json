{
    "name": "oaao/oaao-ai-tooling",
    "description": "Dev tooling (php-cs-fixer, phpstan hooks) for the oaao.ai-v1 backbone. NOT a runtime dependency — backbone modules load via Razy autoloader.",
    "type": "project",
    "license": "proprietary",
    "require": {
        "php": ">=8.2"
    },
    "require-dev": {
        "friendsofphp/php-cs-fixer": "^3.64"
    },
    "scripts": {
        "cs-check": "php-cs-fixer fix --dry-run --diff",
        "cs-fix": "php-cs-fixer fix"
    },
    "config": {
        "sort-packages": true,
        "optimize-autoloader": true
    }
}
