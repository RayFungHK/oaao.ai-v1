/**
 * @deprecated Merged into {@link ./oaao-asr-settings-panel.js} (Settings → ASR).
 * Kept so stale registry / cache still resolves the module without a hard import error.
 */
export { mountSettingsPanel, teardownSettingsPanel } from './oaao-asr-settings-panel.js';
