# IDE / editor
.cursor/
.idea/
*.swp
*~

# Local env (copy from docker/env.example)
.env

# Python orchestrator
python/.venv/
**/__pycache__/
*.py[cod]
.pytest_cache/

# Node (install via core/vite: npm ci)
**/node_modules/

# Docker bind-mount runtime data (see docker/env.example)
docker/data/

# Razy runtime cache (regenerated)
backbone/data/

# Sibling reference tree — not part of oaao.ai-v1 (see ../development-razy0.4)
development-razy0.4/

# OS
.DS_Store
Thumbs.db

# W2-S2 PHP tooling (dev-only; backbone uses Razy autoloader, not composer at runtime)
/.php-cs-fixer.cache
/vendor/
/composer.lock
docker/env
docker/env.bak.*
