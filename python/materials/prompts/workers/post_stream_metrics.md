<!-- Loaded by worker from prompt_bundle_ref / QueueJobPayload.prompt_material_ref — edit without code deploy. -->
# Post-stream worker bundle

Variables are injected by the orchestrator (conversation_id, purpose_id, …).
